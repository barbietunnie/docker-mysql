#!/bin/sh

USERNAME=${USERNAME:-'ruser'}
PASSWORD=${PASSWORD:-'strategic-kittens411'}
DATABASE=${DATABASE:-'*'}
REMOTE=${REMOTE:-'%'}

service mysqld start

sleep 2

mysql -e "GRANT ALL PRIVILEGES ON $DATABASE.* TO '$USERNAME'@'$REMOTE' IDENTIFIED BY '$PASSWORD'"

service mysqld stop

mysqld_safe
